{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3 python3-venv && python3 -m venv /opt/venv && /opt/venv/bin/pip install --upgrade pip && cd backend-python && /opt/venv/bin/pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend-python && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}